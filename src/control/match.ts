/** AD-9: match phase — match idle Creeps to available Jobs */
import type { TakenSet } from "./taken";

// TODO(Story 3.4): match idle Creeps to open Jobs using takenSet capacity,
// scoring by tier → within-tier priority → distance.
export function match(_takenSet: TakenSet): void {
  // Empty implementation — logic arrives in Story 3.4
}
