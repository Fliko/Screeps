---
topic: screeps_ai architecture as build substrate — Blackboard control cycle (Stage 1) + config-driven Node/Pool scheduler pivot (Stage 2), distilled from ARCHITECTURE_SPINE.md, S2 PRD, and epic-6 retro
updated: 2026-08-17T10:13
---

- (capability) CAP-1 Job Board: Producers emit exactly one Job per world object needing work, tagged with a Node, rebuilt from world state every Tick with zero persistence (S1 4.1, AD-1/2/3/10/11). success: Board fully regenerates each Tick from world state; every needy structure carries exactly one Job; no Board data survives past its Tick
- (capability) CAP-2 Contract Lifecycle: a Creep's Contract is exactly one jobId string (type:node:targetId); validators clear it only on S1 FR-9 invalidity; non-Matching delivery paths (e.g. DYING-unload) never target a structure that already carries a live incoming Contract (S1 4.2, AD-2/AD-4 amended). success: a committed-but-traveling worker's target is never sniped by another delivery path mid-flight
- (capability) CAP-3 Node-Gated Matching and Workforce Pools: idle Creeps are ranked by Node Priority -> within-Node balancer (LEAST_FULL or STICKY) -> distance; a Node's live NumWorkers() caps its colony-wide headcount, counted via the Job id's node prefix against current Contracts (S1 4.3 + S2 4.3 FR-8/9/10/11, AD-7 amended/AD-11/AD-13). success: SM-2 - over a rolling 1000-Tick window, no Job type with open eligible demand goes fully unserved while a lower-priority pool is at capacity and a higher-priority pool has spare uncommitted capacity
- (capability) CAP-4 Spawn Management: population targets, proactive replacement, and Reserved-slot target selection all consult the same per-Node config (Priority/NumWorkers/balancer) Matching uses; Reserved Contracts written at spawnCreep are included in the Tick's taken-set (S1 4.4, AD-2/5/7/9). success: a Reserved slot never looks vacant to Spawn while a Spawning Creep already holds it - no double-queue
- (capability) CAP-5 Generalist and Specialist Economy: one behavior file per Job type serves both generalist and specialist bodies; a body-kind is eligible for a Node only if its tolerations list includes the Node's taint, checked alongside existing body/requirements eligibility (S1 4.5/4.7, AD-1/4/8/13). success: a specialist body's Node never silently accepts an unrelated body-kind; a generalist body still serves every Node it tolerates
- (capability) CAP-6 Continuous Workforce Graduation: each Node's NumWorkers()/Priority() is a pure function of a world-state-summary (RCL, structure counts, population), evaluated fresh every Tick, replacing the retired discrete generalist/specialist Era; a Node's population-target function may return nonzero for a reduced-capability specialist-lite role as soon as its own minimal precondition is met (S1 4.6 superseded + S2 4.2 FR-6/7/12, AD-5 amended/AD-12). success: SM-1 - from a fresh room, with the operator only placing Container sites once, workforce composition converges to every Node's NumWorkers() target within a bounded observation window, demonstrated on the local server first
- (capability) CAP-7 Local Development Server: operator bootstraps a private Screeps server with one command, resets the room to fresh RCL1 without restarting the process, deploys the identical dist/main.js build artifact used for sim/shard, and runs ticks unthrottled; it becomes the default dev-loop target ahead of PTR/live (S2 4.1 FR-1..5, AD-14). success: a full generalist-to-graduated run compresses from hours/days on live shard to minutes locally; every per-story sim-room verification step can run against the local server first
- (capability) CAP-8 Config Experimentation Surface: every threshold, cap, priority function, taint, and balancer choice governing colony strategy lives in one per-room per-Node table in config.ts, successor to JOB_POLICY_TABLE (S2 4.4 FR-13, AD-12). success: SM-3 - each of the three named example strategies (first-6-workers-all-fill; specialist-lite-on-first-container; LEAST_FULL-balanced-N-extensions) is expressible as a config edit alone, with zero change to control/match.ts or any producer
- (capability) CAP-9 Movement Choke Point: every Creep move goes through one movement helper with explicit opts; stuck (position unchanged N Ticks AND fatigue==0) triggers one ignoreCreeps re-path then reverts to default opts (AD-8, unchanged by Stage 2). success: no behavior file calls move/moveTo/moveByPath directly outside the helper
- (direction) input: user shared ARCHITECTURE_SPINE.md (screeps_ai, 2026-08-16, status final) via /bmad-spec - Blackboard paradigm + Stage 2 Node/Pool scheduler pivot
- (note) read ancillary linked sources for capability Why/success context: S2 PRD (prd-screeps_ai-2026-08-16/prd.md), its addendum.md, epic-6-retro-2026-08-16.md
- (decision) no prior spec folder existed for slug screeps_ai - this is a create, not an update; folder _bmad-output/specs/spec-screeps_ai/
- (decision) ARCHITECTURE_SPINE.md kept as adopted companion, not absorbed into sources - downstream/dev cites AD-N ids directly and ongoingly, it is not mere backstory
- (decision) S1 PRD (prd-screeps_ai-2026-08-07/prd.md) and its addendum.md kept as adopted companions, unabsorbed - Stage 1 FR-1..30 detail not independently re-derived in this run; spec relies on the spine's carried-forward AD summary of Stage 1 decisions
- (decision) S2 PRD, S2 addendum, epic-6-retro-2026-08-16 treated as fully-absorbed sources - Vision/Why narrative backstory is wrapper ceremony, not load-bearing; their FR/SM/finding content is distilled into capabilities, constraints, non-goals, success signal
- (constraint) Blackboard role separation: every module is exactly one of world/control/board/agents/state; dependencies flow one way (world writes board; control+agents read world+board; nothing calls control); a new capability is a new file inside a role dir, never a cross-role module (AD-1)
- (constraint) Field-level write ownership: world/ regenerates Board per Tick; only control/ sets Contracts (at spawn or claim); agents/ write only their own creep.memory.move; a Creep's own validator may only clear its Contract, never set one; state/ holds no business logic (AD-2)
- (constraint) Board is a per-Tick derived projection: recomputed from world state every Tick, no Board data persists, nothing references a previous Tick's Board (AD-3)
- (constraint) Zero colony-level Memory persistence outside Memory.creeps: spawn demand and population targets are derived fresh from world state every Tick, never remembered (AD-5)
- (constraint) Volatile caches live only on global, rebuilt lazily from world state, never written to Memory, never cache Game object references - ids/plain data only (AD-6)
- (constraint) No pathfinding (PathFinder/findPath/moveByPath) inside the Matching scoring path - distance is Chebyshev range only, from the single world/ distance service (AD-7)
- (constraint) A Node's Priority() is evaluated once per Node per Tick, not once per Job - every Job under that Node in a given Tick carries the same Tick's single Node-Priority value (AD-7, CPU-bound: one call per Node not per Job)
- (constraint) Config-supplied functions (NumWorkers, Priority, any per-Node function) take exactly one argument - an already-derived world-state-summary - and read nothing else: no Game/Memory calls, no closures over mutable state (AD-12)
- (constraint) The control cycle runs exactly one pass per Tick in fixed order: generate -> taken-set -> validate -> match -> spawn; the taken-set derives from all Creeps' Contracts including Spawning ones (AD-9)
- (constraint) Game API reads exist only inside world/; all other modules, including validators and config.ts, read the game only through world/'s exposed snapshot (AD-10)
- (constraint) The local private server introduces no new persistence and no bot-code branching: same dist/main.js, same config.ts, across all three deploy targets - sim room, local server, official shard (AD-14, NFR-1)
- (constraint) Pool/stage evaluation must not raise per-Tick CPU cost materially above Stage 1's baseline, including through stage-transition spikes (S2 NFR-3, SM-C1)
- (decision) non-goal: no new Job types beyond mine/fill/build/upgrade (S2 PRD 8)
- (decision) non-goal: no automated/headless test harness against the local server - manual fast-iteration only (S2 PRD 8, addendum)
- (decision) non-goal: no UI/dashboard beyond existing console logging conventions (S2 PRD 8)
- (decision) non-goal: no multi-room support - config is keyed by room profile to leave room for it, but Stage 2 ships exactly one default profile (S2 PRD 8, AD-12)
- (decision) non-goal: no hot-reload of config without a rebuild/push - redeploy-per-change accepted as an iteration cost (S2 PRD 8)
- (decision) non-goal: no discrete stage/phase value anywhere in the design - Era is fully retired, not replaced by another discrete concept; a decorative non-authoritative phase label for logging only is Deferred, not built (AD-5 amendment)
- (decision) non-goal: no behavior-level unit tests for MVP - sim-room/local-server manual verification only, rejected for MVP, ripe for revisit now a private server exists (spine Deferred)
- (decision) success signal: SM-1 - from a fresh room, operator only placing Container sites once, workforce composition converges to every Node's NumWorkers() target within a bounded window, shown on local server first, confirmed on PTR/shard before it matters
- (decision) success signal: SM-2 - over a rolling 1000-Tick window, no Job type with open eligible demand goes fully unserved while a lower-priority pool sits at capacity and a higher-priority pool has spare uncommitted capacity
- (decision) success signal: SM-3 - all three named example strategies (first-6-fill; specialist-lite-on-first-container; LEAST_FULL-balanced-N-extensions) are each expressible as a config edit alone, no change to control/match.ts or any producer
- (decision) success signal: SM-C1 counter-metric - sustained CPU average stays under the account limit with headroom, including through stage-transition spikes
- (assumption) FR-4 'unthrottled tick cadence' has no numeric target - treated as 'as fast as the local server naturally runs, uncapped' (S2 PRD 9, carried as-is)
- (assumption) Stage 1 FR-1..30 detail is not independently re-derived in this spec run; relies on ARCHITECTURE_SPINE.md's carried-forward AD summary of Stage 1 decisions, with the S1 PRD+addendum linked as adopted companions for full traceability
- (question) do Epic 6's still-backlog stories (6-7 deprecation/graceful-degradation, 6-8 evolution-observation-MVP-exit) get reframed as Stage 2 stories under the Node model, or formally dropped? (S2 PRD 9 OPEN, epic-6-retro Open Questions - explicitly deferred to the epics/stories phase)
- (question) is the decorative non-authoritative 'current phase' label (console-log only, zero effect on scheduling) worth building once the continuous Node functions are live, or does it stay deferred indefinitely? (spine Deferred)
- (decision) companion structure: architecture-diagrams.md (3 mermaid flowcharts, spec-authored), node-pool-model.md (Kubernetes-concept mapping + resolved config shape + balancer/taint semantics, spec-authored), stack.md (toolchain versions + deploy targets, spec-authored), conventions.md (naming/data/state/config/lifecycle/logging tables + structural seed + capability-to-architecture map, spec-authored), deferred.md (Deferred roster with rationale, spec-authored); ARCHITECTURE_SPINE.md + S1 PRD + S1 addendum as adopted companions
- (constraint) Test strategy for the Node-gated cascade must include at least one population-distribution test class - N Jobs of type A + M Jobs of type B + population P competing across pools - not only single-pair scoring tests (S2 NFR-4; epic-6-retro Finding 3 names this exact coverage gap as what let Stage 1's starvation ship untested)
- (constraint) Stage 2 ships in fixed order: the local private server (CAP-7) first, the Node/Pool scheduler (CAP-3/6/8) second - the scheduler is tuned and observed against the local server; building it against the live shard's tick cadence would slow all of Stage 2's development, not just its later use (S2 PRD 3 Sequencing)
- (event) self-validate pass 1 (coherence, rules 1-6+8): PASS - all 9 capabilities carry intent+success; intents are WHAT-level; 14 constraints all bend design (none decorative); 7 non-goals explicit; success signal concrete (SM-1/2/3/C1 with numeric/named criteria); CAP ids unique and stable; prose lean throughout
- (event) self-validate pass 2 (preservation): found 2 gaps on first walk - S2 NFR-4 (population-distribution test coverage requirement) and S2 PRD 3 Sequencing (local server ships before scheduler) were not yet landed - both added as Constraints. Wrapper-only content confirmed dropped: S2 PRD 0 wording/numbering notes (process meta), SM-C2 (process-integrity counter-metric, about workflow not the software), retro Finding 4 (fill.maxWorkers=6 historical deviation, moot once Stage 2 lands), retro Action Items 4-5 (doc housekeeping, sprint-status housekeeping), addendum's soft FR-2 reset-mechanism either/or (unresolved implementation detail, not a source-mandated claim). Re-walked after fix: all remaining FR/NFR/SM/Non-Goal/Glossary/Finding items from S2 PRD, addendum, and epic-6-retro now land in SPEC.md or a companion - PASS
- (event) spec finalized: SPEC.md + 5 spec-authored companions (architecture-diagrams.md, node-pool-model.md, stack.md, conventions.md, deferred.md) + 3 adopted companions (ARCHITECTURE_SPINE.md, S1 prd.md, S1 addendum.md); 3 sources fully absorbed (S2 prd.md, S2 addendum.md, epic-6-retro-2026-08-16.md)
- (decision) resolved: Epic 6 backlog stories (6-7-deprecation-graceful-degradation, 6-8-evolution-observation-mvp-exit) are dropped, will not be executed - they were written against the retired discrete-Era design
- (decision) resolved: the decorative 'current phase' label idea is rejected outright, not merely left deferred - there is no discrete phase in Stage 2's design, so a phase label would be meaningless. Replaced by a new capability: log the current Tick's fully-resolved per-Node config values (NumWorkers()/Priority() outputs, post-evaluation) - meaningful operator visibility that doesn't require a phase concept to exist
- (capability) CAP-10 Resolved-Config Observability: the console log surfaces each active Node's fully-resolved NumWorkers()/Priority() output values for the current Tick, computed fresh each Tick same as the functions themselves - no discrete phase, no persistence. success: an operator reading the console for any Tick can see every Node's live population target and priority without inferring them from Contract counts by hand
- (event) SPEC.md updated: both open questions resolved (6-7/6-8 dropped; phase label rejected in favor of CAP-10). Open Questions section now empty, removed from SPEC.md per template. deferred.md's phase-label bullet replaced with a resolution note pointing to CAP-10. conventions.md Observability row updated to mention resolved-config logging
- (event) spec re-derived: CAP-10 added, both open questions resolved and removed, deferred.md + conventions.md updated to match. 10 capabilities, 16 constraints, 7 non-goals, 0 open questions, 2 assumptions
- (direction) user: all logging must be gated, similar to a DEBUG log-level flag - default shows nothing but critical errors; verbose output (CPU metering, CAP-10 resolved-config values) only appears when the operator raises the level to debug
- (capability) CAP-11 Gated Logging: all console output beyond critical errors is gated behind a single config-set log level; default level suppresses everything except critical errors; CPU metering and CAP-10's per-Tick resolved-config output only emit when the operator raises the level for debugging. success: at default log level, a Tick's console output contains nothing but critical-error lines (if any); raising the level surfaces CPU metering and resolved-config lines with no other code change
- (constraint) All non-critical logging (per-phase CPU metering, per-Tick resolved-config values, any future debug output) must check a single config.ts log-level flag before emitting; default level is errors-only (CAP-11)
- (decision) CAP-10's success criterion amended to read 'when debug logging is enabled' - resolved-config values are debug-gated output, not always-on, per CAP-11
- (event) SPEC.md updated: CAP-11 Gated Logging added; CAP-10 success amended for gating; Constraints gained the log-level-gate rule; conventions.md Logging and Observability rows updated to reflect gating
- (event) spec re-derived: CAP-11 Gated Logging added, CAP-10 success amended, log-level-gate constraint added, conventions.md rows updated. 11 capabilities, 17 constraints, 7 non-goals, 0 open questions
- (decision) story breakdown: user merged the two local-server stories into one (CAP-7 ships as a single story, FR-1..5 together); checkpoint policy = spec_checkpoint on foundational/breaking-change stories only, done_checkpoint at the end of each numbered group only
- (decision) story 1: Local Private Server - bootstrap, fast reset, identical deploy artifact, unthrottled cadence, default dev loop (CAP-7 FR-1..5, AD-14). done_checkpoint=true (end of local-server group). no spec_checkpoint
- (decision) story 2: Node Tagging and Amended Job/Contract Id Grammar - NodeName union in board/job.ts, Producers tag Jobs with node, id becomes type:node:targetId (CAP-1 amend/CAP-2 amend, AD-11/AD-4). spec_checkpoint=true (schema-changing). no done_checkpoint
- (decision) story 3: Contract-Commitment Protection for Non-Matching Delivery Paths - DYING-unload and future delivery paths prefer targets with no live incoming Contract (CAP-2 new rule, AD-4). done_checkpoint=true (end of contract-layer group). no spec_checkpoint
- (decision) story 4: Node Config Surface - per-room per-Node table in config.ts, pure NumWorkers/Priority functions over a world/-owned world-state-summary type (CAP-6+CAP-8, AD-12). spec_checkpoint=true (breaking-change, replaces JOB_POLICY_TABLE). no done_checkpoint
- (decision) story 5: Node-Gated Matching Cascade and Balancer - Node Priority -> balancer (LEAST_FULL/STICKY) -> distance, NumWorkers() colony-wide cap via node-prefix Contract count, includes NFR-4 population-distribution test class (CAP-3, AD-7 amended). spec_checkpoint=true (central mechanism change). no done_checkpoint
- (decision) story 6: Taints and Tolerations Eligibility Gate - Node taint tag + body-kind tolerations list, checked alongside body/requirements eligibility (CAP-5, AD-13). no checkpoints
- (decision) story 7: Reserved-Mode Spawn Node Consultation - control/spawn's Reserved target selection consults the same Node config as Matching; NumWorkers() caps total headcount whether Reserved or Pulled (CAP-4, AD-7/AD-2/AD-9). done_checkpoint=true (end of scheduler-core group). no spec_checkpoint
- (decision) story 8: Retire Discrete Era - remove deriveEra and the generalist/specialist value from world/snapshot.ts and all consumers, lands after stories 4 and 5 are live (AD-5 amendment cleanup). spec_checkpoint=true (deletion of shipped mechanism). no done_checkpoint
- (decision) story 9: Gated Per-Tick Observability Logging - log-level flag built first, CPU metering and CAP-10's resolved-config line wired behind it in the same story, default errors-only (CAP-10+CAP-11). done_checkpoint=true (end of final group). no spec_checkpoint
- (event) stories.yaml written: 9 stories, execution order 1-9. Validity check PASS - all entries have id/title/description; ids 1..9 unique, prefix-free, quoted strings; no status field. spec_checkpoint on stories 2,4,5,8 (schema/mechanism/breaking changes); done_checkpoint on stories 1,3,7,9 (end of each group: local-server, contract-layer, scheduler-core, final)
