---
scope: screeps_ai MVP colony-bot engine (PRD FR-1..FR-30, NFR-1..NFR-4)
purpose: build substrate for a solo author implementing by hand; keeps future epics consistent
altitude: feature
updated: 2026-08-08T12:12
---

- (event) Architecture run started: create intent, coaching path; input = PRD package prd-screeps_ai-2026-08-07; deliverable = spine only (user confirmed)
- (constraint) From PRD: author writes every line of code — stack/tooling must stay hand-learnable, no codegen-dependent frameworks
- (constraint) From PRD NFR-4: Screeps runtime only — single-threaded per-Tick execution, JSON Memory, CPU limit + bucket; no external services; JavaScript or languages compiling to it
- (constraint) From PRD: test venues = official simulation room + official World shard; no private server
- (constraint) From PRD/addendum: blackboard coordination model (Job Board, Producers, Matching, sticky Contracts) is settled product language — architecture maps to it, does not reopen it
- (direction) User learning goal: surface decision rationale and weighed alternatives in conversation + memlog (spine stays terse); also wants meta-guidance on directing AI effectively
- (question) User concern: does TypeScript-compiled output risk NFR-1 (CPU discipline) vs plain JS? Answered in-conversation: types erase at compile, runtime ≈ hand-written JS on modern V8 targets; real CPU risks are algorithmic. Stack decision remains open.
- (version) Verified against docs.screeps.com 2026-08-07: CPU = ms/tick, 20ms fixed baseline (GCL-scaled with CPU Unlock); bucket accrues to 10,000, bursts up to 500/tick; pathfinding named by docs as the canonical burst/defer operation. TS type-erasure mechanics stated from language knowledge (stable).
- (decision) Paradigm confirmed (user): Blackboard — Producers as knowledge sources, Job Board as the blackboard, Matching + Spawn Management as control; per-Tick control cycle per addendum ordering. Rejected: layered (doesn't encode stickiness), actor (breaks FR-13 central claim lock), ECS (ceremony without payoff)
- (decision) Stack direction (user): TypeScript with hand-rolled minimal build; screeps-typescript-starter as reference only; types make Job/Contract data structures self-checking (author's data-structures learning goal); version pins to be verified before binding
- (decision) Core invariant refined through coaching: reads are free (world/ and board/ are read-only services), writes are owned — world regenerates the Board per Tick; control writes Contracts/taken-set/spawn-queue/era-latch via state/ schema; agents write only their own Creep memory; state/ owns schemas + serialization, no business logic. Supersedes the looser world→board→control→agents import direction
- (decision) Evolution module confirmed in control/ (user): reads world/ for the FR-23 trigger facts, writes the era latch via state/ — legal under reads-free/writes-owned
- (decision) Travel-cost service placed in world/ (geometry knowledge source), consumed read-only by control (Matching) and agents (movement); cache is VOLATILE on global, never Memory (NFR-2); corollary rule: volatile caches must be lazily rebuildable any Tick since global can reset at any time. User's initial state/ persistence placement adjusted with rationale
- (version) Toolchain pins verified via npm registry 2026-08-07: typescript 7.0.2, esbuild 0.28.1, @types/screeps 3.4.0, screeps-api 2.1.0, vitest 4.1.10; rollup-plugin-screeps 1.0.1 rejected as stale (2020, screeps-api v1)
- (decision) Distance/travel-cost: no pathfinding in the Matching/scoring path; all distance answers from the single world/ distance service; MVP = Chebyshev getRangeTo heuristic, zero cache. Real-path costs + volatile global caching DEFERRED to Phase 2 scale (multi-room). Trigger: user's challenge exposed the cache as over-design at MVP scale
- (direction) User feedback on working style: no praise framing, no presumption of correctness — engage critically by default; apply for the rest of the run
- (question) User correction: movement pathing (moveTo/moveByPath/move) is a second, larger pathfinding consumer beyond Matching scoring — my cache discussion under-scoped it to Matching. Movement design fork opened.
- (version) docs.screeps.com/api verified 2026-08-07: PathFinder+CostMatrix, Room.findPath, getRangeTo/findClosestByPath present; RawMemory segments (100KB strings, up to 10MB) = future route-cache medium without JSON parse tax. moveTo internals (reusePath default 5, path cached in creep.memory._move) from stable API knowledge — flagged for implementation-time verification
- (decision) Movement (user chose B): all movement via one agents/ movement helper wrapping moveTo with explicit opts; no behavior calls move/moveTo/moveByPath directly. Stuck handling = PER-CREEP escalation, not colony toggle: stuck := position unchanged N consecutive Ticks AND fatigue==0 → re-path once with ignoreCreeps:true, then revert. Colony-wide ignoreCreeps toggle rejected (flapping, off-ramp semantics, NFR-2 state). Thresholds in Ticks, not minutes; N joins MVP constants. Owned routing (moveByPath + route cache) stays Deferred to Phase 2+
- (decision) 4a Contract record (user): jobId only, all else derived — with corrections bound here: derive predicate is source-iff-empty (not serve-iff-full, which ping-pongs on partial carry); Job id grammar bound as type:targetId so validation is Board-free via Game.getObjectById; creep.memory also hosts movement-helper state (lastPos/stuck) and engine-owned _move
- (decision) 4b persisted state (user): ZERO colony-level Memory — spawn queue/counters derived per Tick; era derived per Tick (degrade-on-attack), PRD FR-27 amended accordingly; per-Creep memory only (contract + move state). Validation backoff/throttling logged as Phase 3 tuning lever (kin to throttled Board regeneration in addendum)
- (decision) creep.memory.move schema: {lastPos (packable y*50+x), stuck counter} — lastPos is mandatory because stuck := position-unchanged-over-N-ticks is a temporal predicate requiring minimal past-state; Room.getEventLog() alternative rejected (more CPU + more code per Tick)
- (decision) Test strategy confirmed (user): unit-test decision logic only (producers, matching/scoring, spawn policy, era derivation, validators) against fake world snapshots; behaviors verified in sim room, NOT unit-tested; revisit strategy if a private server is ever adopted. Enabler rule: all world reads go through world/ snapshot so tests never mock the Game API
- (version) Static-analysis pins verified via npm registry 2026-08-07: eslint 10.8.1, typescript-eslint 8.66.0 (peer: typescript <6.1.0 — blocks TS7), prettier 3.9.6, biome 2.5.7 (alternative), typescript 5.9.3 (newest 5.9.x; TS 7.0.2 is latest but incompatible with typescript-eslint). Node engine floor: ^22.13 (eslint) + 22.x||24.x (screeps-api) → Node 22 LTS
- (decision) Static analysis (user): Biome 2.5.7 single-tool lint+format — day job is Go/C + Python throughput pipeline, TS-stack transfer is not a factor; typescript 7.0.2 as typecheck gate; eslint/typescript-eslint/prettier dropped. Caveat logged: @types/screeps under TS7 unverified, fallback pin typescript ~5.9.3
- (event) Spine distilled from memlog: Blackboard paradigm; AD-1..AD-10 (roles, reads-free/writes-owned, derived Board, contract/id grammar, zero colony persistence, volatile caches, no-pathfinding-in-scoring, movement choke point, fixed cycle order, world-reads-only); conventions; verified stack; structural seed; capability map; Deferred list
- (change) Reconcile fixes applied: AD-7 gains tier→within-tier-priority→distance ordering (FR-24 expressible as data); config.ts convention now carries the policy table (tiers/within-tier/maxWorkers/Reserved-vs-Pulled), named TTL constants, MVP Body compositions, values pinned at first consuming story; era derivation moved into world/ snapshot (G4 contradiction closed, AD-1 intact); CPU metering convention added (Game.cpu.getUsed per phase, sim observation at MVP exit); creep lifecycle + DYING unload named as convention; Job schema made explicit; behavior-FR sim verification noted in Deferred
- (event) lint_spine.py clean (0 findings) pre-gate
- (change) Gate fixes applied: AD-2 field-level write ownership (control sets Contracts via spawnCreep-initial-memory or Matching; validators may only clear; agents own .move; engine owns ._move) — the HIGH contradiction; AD-10 names control/spawn as spawnCreep issuer; AD-1 gains one-way dependency rule; AD-5 names Producer era-gating via snapshot; AD-9 names taken-set residence (main.ts, never stored); Build convention added (CJS single-file es2022, no minify in dev); Node pin moved to 24 LTS; Deferred notes for moveTo-internals verification and RawMemory/AD-6 amendment. Version lens: PASS with flags — all pins registry-current
- (event) Adversarial lens run inline (subagent dispatch failed twice — auth/socket errors); found 1 high (taken-set must include Spawning Creeps), 2 medium (stale object-ref caching on global; aggregate-vs-per-object Jobs), 1 low (validator carry-state clearing), 1 info (Phase-3 config read semantics)
- (change) Adversarial fixes applied: AD-9 taken-set includes Spawning Creeps (prevents Reserved-slot double-spawn); AD-6 forbids caching Game object references (ids/plain data only); AD-3 binds one-Job-per-object (no aggregates); AD-4 validators clear only on FR-9 invalidity, never carry state; Deferred gains Phase-3 config read-semantics note
- (decision) Triage: 0 phase-blockers for epics/stories; deferred verifications with revisit conditions — @types/screeps-under-TS7 at first build (fallback typescript ~5.9.3), moveTo engine internals at the movement-helper story
- (event) spine finalized
- (decision) Constraint superseded: 'author writes every line' reversed (user) — AI codegen in scope with human review and human-only commits; stack/tooling decisions unaffected (author-agnostic); dev-story/dev-auto now part of the intended flow
